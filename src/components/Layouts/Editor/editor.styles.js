import styled from '@mui/system/styled'

export const TextArea = styled('textarea')((props) => ({
  width: '100%',
  height: '100%',
  border: 'none',
  outline: 'none',
  resize: 'none',
  padding: '1rem',
  fontSize: '1.4rem'
}))
